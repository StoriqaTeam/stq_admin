server {
    listen 80 default;

    root /app;

    location /storiqatools {
        index /admin/index.html;
        try_files $uri /admin/index.html;
    }

    location /healthcheck {
        return 200;
        access_log off;
    }
}
